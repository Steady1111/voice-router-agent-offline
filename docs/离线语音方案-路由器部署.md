# 离线语音控制方案（路由器部署）

> 架构师：AI架构师 | 日期：2026-06-23 | 更新：2026-06-25
> 目标设备：128MB MIPS架构路由器（可用内存约50MB）

---

## 一、架构选型：为什么是 Linux 原生方案？

核心目标：**不影响路由器网络吞吐**的前提下，实现精准、低延迟的离线语音控制。

### 三套候选架构全景对比

| 评估维度 | 方案 A：外挂 MCU 芯片 | 方案 B：Linux 原生服务（✅ 选中） | 方案 C：端侧大模型 |
|---|---|---|---|
| **系统架构** | 独立外挂芯片 + 裸机/RTOS | 路由器主 SoC 复用 + Linux Daemon | 独立 AI 算力板 + 容器化 |
| **内存消耗** | < 1 MB（高价 SRAM） | **10-50 MB（廉价 DDR）** | > 1-4 GB（LPDDR） |
| **硬件 BOM** | +独立语音芯片 | **+麦克风阵列** | +昂贵边缘计算主板 |
| **交互能力** | 固定命令词（100-300条） | **数百词条 + 复杂状态机** | 自然语言自由理解 |
| **端到端延迟** | < 300 ms | **500-1000 ms** | 2000-5000 ms+ |
| **业务确定性** | 绝对精确 | **绝对精确** | 存在幻觉风险 |
| **开发门槛** | 高（跨技能栈） | **中（标准 Linux）** | 极高 |

### 决策理由

**放弃 A（MCU）：** 路由器主板已有 SoC 和上百兆 DDR，外挂专用语音芯片是硬件上的重复建设；裸机固件与 Linux 主业务通过低速 UART 通信，OTA 升级和状态同步极度繁琐。

**放弃 C（LLM）：** 最小量化版仍需 >1GB 内存，百元路由器成本直冲千元级；5-15W 推理功耗在无主动散热的密闭空间内会导致芯片积热降频；家电控制是强确定性指令映射，LLM 的幻觉是不可接受的。

**选中 B：** 复用现有 DDR 内存（边际成本极低），标准 Linux 生态（ALSA 音频、UBus IPC），cgroups 资源限制保障网络转发优先。用几十兆廉价内存换取零额外芯片成本 + 统一软件栈 + 可控确定性。

> ⚠️ MIPS 单核 580-880MHz 算力瓶颈：ASR 推理可能占用 50-80% CPU。应对：VAD 静音检测 + cgroups CPU 配额；推荐硬件基线 256MB 双核路由器。

---

## 二、内存预算

### 2.0 路由器量产架构（推荐）

预研阶段用 Mac + ESP32 WebSocket 验证逻辑；**烧录路由器时切换为下列架构**：

```
USB-I2S 麦克风
      │
      ▼
   ALSA 采集
      │
      ▼
voice-routerd  (python3 -m voice_router_lite router)
      │
      ├─ 待机: KWS + NLU(INT8) 常驻
      ├─ 唤醒: 卸载 KWS → fork ASR 子进程 → 识别完进程退出
      └─ ubus → OpenWrt 网络/系统/GPIO 控制
```

| 策略 | 作用 |
|------|------|
| `model_serial_exclusive` | KWS / ASR **不同时驻留** DDR |
| `asr_subprocess` | ASR 在**独立子进程**，退出后内核回收全部 ASR 内存 |
| `VOICE_ROUTER_ASR_WORKER` | 可选 C++ 原生 worker（`native/`），峰值再省 ~24MB |
| `prefer_int8_nlu` | NLU 使用 INT8 模型（~4MB 替代 ~11MB） |
| `procd` 托管 | 见 `deploy/openwrt/voice-router.init` |

启动命令：`python3 -m voice_router_lite router --config deploy/openwrt/voice-router.json`

### 2.1 分阶段内存（128MB 路由器，优化后）

| 阶段 | 加载组件 | 内存占用 | 备注 |
|------|---------|---------|------|
| 待机 | 系统 + KWS + NLU(INT8) | **~72MB** | 无 ASR 常驻 |
| 唤醒峰值（Python ASR） | + ASR 子进程 | **~89MB** | KWS 已卸载，ASR 进程独立 |
| 唤醒峰值（C++ ASR） | + 原生 worker | **~65MB** | 无 Python 解释器开销 |
| 识别后 | ASR 进程退出 | **~72MB** | 自动回落 |

| 项目 | 磁盘/运行时 |
|------|------------|
| 系统基础（OpenWRT + Python） | ~60MB |
| ASR（zipformer zh 14M mobile） | **24MB** 文件 / 子进程 ~25MB |
| KWS（sherpa-onnx 3.3M） | **5MB** 文件 / ~8MB 运行时 |
| NLU INT8 | **~3MB** 文件 / ~4MB 运行时 |
| 音频缓冲（2秒@16kHz） | ~2MB |

> **重要**：必须使用 `download_models.sh` 下载的 **zh 14M mobile** 模型（~25MB），勿使用 bilingual 大模型（~189MB）。NLU 后续可通过 INT8 量化降至 ~3MB。

### 2.2 关键取舍

| 决策 | 原因 | 节省内存 |
|------|------|---------|
| ❌ 放弃实时TTS → 预录制音频拼接 | TTS模型 >30MB | ~30MB |
| ✅ 保留 KWS 唤醒词 | 用户体验优于按键触发，10MB 可控 | — |
| ✅ 字符级 CNN+LSTM | 比 Transformer/BERT 小一个量级 | — |

---

## 三、技术选型

### 3.1 ASR：sherpa-onnx Zipformer（25MB）

| 方案 | 模型大小 | 运行时 | 中文 | MIPS兼容 | 结论 |
|------|---------|--------|------|----------|------|
| sherpa-onnx Zipformer | 25MB | ~5MB | ⭐⭐⭐⭐ | ✅ ONNX | **选中** |
| Paraformer | 50-200MB | >50MB | ⭐⭐⭐⭐⭐ | ❌ FunASR生态 | 太重 |
| Vosk small | 42MB | ~40MB | ⭐⭐⭐ | ⚠️ 需编译 | 内存大 |
| Whisper tiny | 78MB | ~300MB | ⭐⭐⭐⭐ | ❌ 太大 | 不可行 |

### 3.2 NLU：CNN+LSTM（10.7MB / 量化目标 2-3MB）

| 方案 | 模型大小 | 意图分类 | 槽位提取 | 结论 |
|------|---------|---------|---------|------|
| **CNN+LSTM** | 10.7MB (fp32) | 16类，99.8% | BIO序列标注，37标签 | **选中** |
| FastText | 几百KB | ✅ | ❌ 靠规则引擎 | 无槽位能力 |
| Transformer tiny | 10MB | ⭐⭐⭐⭐⭐ | ✅ | 内存不够 |
| 纯规则引擎 | 0MB | ⭐⭐⭐ | ❌ | 兜底用 |

### 3.3 语音反馈：预录制音频拼接（0MB运行时）

录制 20-30 条常用回复，根据 NLU 结果选择播放。

### 3.4 同行方案对比：Paraformer + FastText

| 层级 | 本方案 | Paraformer + FastText |
|------|--------|----------------------|
| ASR | Zipformer 25MB（MIPS 可交叉编译） | Paraformer 50-200MB（MIPS 不兼容） |
| NLU 意图 | CNN+LSTM，16类 99.8% | FastText，仅分类 |
| NLU 槽位 | ✅ BIO 一步到位 | ❌ 依赖规则引擎正则/关键词 |
| ASR 容错 | ✅ 训练注入错误，天然鲁棒 | ❌ n-gram 对错字零容忍 |
| 部署栈 | 单一 ONNX Runtime | 两套推理栈 + 规则引擎 |

#### 为什么 Paraformer + FastText 方案不具备参考价值

以下从工程落地的客观约束逐层分析，这些判断基于物理限制和架构规范，而非主观偏好。

**一、硬件兼容性硬伤 —— 无法在目标芯片上运行**

本方案 Zipformer 裁剪至 25MB，且支持 MIPS 交叉编译，是真正面向低成本硬件的生存逻辑。对方 Paraformer 体积在 50-200MB，且 **MIPS 不兼容**。这意味着对方所谓的"跑通"，大概率是在 PC 或资源极其宽裕的 ARM 验证板上完成的。在一个目标定为 MIPS 架构的项目里，拿一个无法在 MIPS 上编译运行的方案作为进度标杆，这在架构评审阶段就应被直接否决。

**二、部署栈复杂度失控 —— 内存碎片与调度冲突**

嵌入式环境对系统开销极其敏感。本方案是单一 ONNX Runtime，部署链路干净。对方方案需要两套推理栈加一套规则引擎。在受限的内存环境下，每增加一套运行环境，内存碎片和系统底层调度冲突的概率呈指数级上升。这种架构在长期运行中几乎必然导致系统崩溃。

**三、维护成本与容错率的错判**

路由器场景的实体提取（设备名、带宽值、安全策略等）是动态变化的。对方采用 FastText 仅做分类，后续强依赖正则和关键词。这种方案在早期跑固定 Demo 时显得很快，但只要业务场景开始泛化，规则引擎就会变成一个永远填不完的逻辑黑洞。本方案 CNN+LSTM 配合 BIO 序列标注一步到位，并且做了错误注入训练——这是典型的将系统复杂度消化在训练阶段，从而保障端侧推理的极简与鲁棒。

> **定性结论：对方做的是一个无视物理约束、只为尽快发声的演示原型。本方案做的是一个符合目标架构、具备长周期维护潜力的工业级系统。** 客观的技术指标和物理限制是任何管理话术都无法掩盖的。

---

## 四、模型训练概要

> 详细设计见 [模型训练方案.md](./模型训练方案.md)，训练执行记录见 [docs/模型训练日志.md](./docs/模型训练日志.md)。
> 训练脚本：`voice_router_lite/nlu/train_nlu.py`（全自动：模板生成数据 → 训练 → ONNX 导出 → 自检）

### 意图设计（16个，路由器核心10 + 智能家居6）

| 类型 | 意图 | 示例指令 | 数量 |
|------|------|---------|------|
| 🔵 路由 | router_reboot | "重启路由器"、"网络卡了重启一下" | |
| 🔵 路由 | router_wifi_restart | "重启WiFi"、"WiFi断了重连" | |
| 🔵 路由 | router_wifi_config | "改WiFi密码"、"切换到5G频段" | |
| 🔵 路由 | router_network_query | "IP地址是多少"、"谁连了WiFi" | |
| 🔵 路由 | router_led_control | "关掉路由器灯" | |
| 🔵 路由 | router_device_manage | "踢掉这部手机"、"把客厅电视拉黑" | **~65%** |
| 🔵 路由 | router_network_diag | "测一下网速"、"延迟多少" | |
| 🔵 路由 | router_system | "检查固件更新"、"备份配置" | |
| 🔵 路由 | router_qos | "限制这台电脑的网速" | |
| 🔵 路由 | router_security | "打开防火墙"、"家长控制打开" | |
| 🏠 家居 | device_control | "打开风扇"、"关闭空调" | |
| 🏠 家居 | device_adjust | "风扇调大一点"、"灯光调暗" | |
| 🏠 家居 | device_query | "风扇开着吗" | **~35%** |
| 🏠 家居 | timer_setting | "10分钟后关闭风扇" | |
| 🏠 家居 | scene_mode | "睡眠模式"、"节能模式" | |
| 🏠 家居 | help | "你能做什么" | |

### 训练结果

| 指标 | 数值 |
|------|------|
| 训练数据 | 2,465 条（模板自动生成） |
| 词汇表 | 291 字符 |
| 意图准确率 | **99.8%** |
| 槽位准确率 | **99.4%** |
| ONNX 模型 | 11MB (fp32) |
| 自检通过 | **26/26** ✅ |

---

## 五、硬件连接

### 5.1 当前方案：ESP32 中继（预研验证中）

```
路由器 (OpenWRT)
  └── WiFi ──→ ESP32-S3
                  ├── INMP441 麦克风（I2S）
                  ├── MAX98357A DAC → 喇叭
                  ├── GPIO → 继电器模块 + 风扇
                  └── I2C → OLED 显示屏
```

ESP32 负责音频采集 + 设备驱动，通过 WiFi WebSocket 将 PCM 音频流发送给路由器语音引擎，接收设备控制指令。

### 5.2 目标方案：路由器直连（生产部署）

```
路由器 (OpenWRT)
  ├── USB → USB-I2S音频模块 → INMP441 麦克风 + MAX98357A DAC → 喇叭
  ├── GPIO → 继电器模块
  └── WiFi → ESP32等扩展设备（可选）
```

> ESP32 中继方案适合预研阶段快速验证；生产部署推荐 USB-I2S 直连以减少延迟和故障点。

---

## 六、实施路线

| 阶段 | 内容 | 时间 | 状态 |
|------|------|------|------|
| **预研模拟** | Mac 服务端 + ESP32 音频采集/设备控制 + WebSocket 联调 | 2-3周 | ✅ 完成 |
| **NLU 训练** | 模板生成数据 → CNN+LSTM 训练 → ONNX 导出 | 1天 | ✅ 完成 |
| **ASR/KWS 集成** | sherpa-onnx + 正确小模型 + Pipeline KWS 状态机 | 1-2天 | ✅ 完成 |
| **端到端验证** | Mac/ESP32：KWS→ASR→NLU→设备控制 | 3-5天 | ✅ 完成 |
| **路由器移植** | MIPS 交叉编译 + ubus 集成 + cgroup 限制 | 3-5天 | ⬜ 待验证 |
| **部署测试** | 路由器端到端 + 72h 稳定性测试 | 3-5天 | ⬜ |

---

## 七、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 内存不足 | 高 | 严重 | NLU INT8量化 →2-3MB；极端情况纯规则引擎兜底 |
| MIPS兼容性 | 中 | 严重 | 提前 QEMU 模拟 MIPS 验证；备选 256MB 双核路由器 |
| ASR准确率不高 | 中 | 中等 | NLU 训练已注入 30% ASR 错误变体（同音/漏字/多字） |
| NLU理解错误 | 中 | 中等 | 模型输出置信度 <0.5 时回退规则引擎兜底 |
| 路由器性能差 | 高 | 中等 | VAD 静音检测减少无效推理；cgroups CPU 配额 |

---

## 附录：技术栈总结

| 层级 | 技术选型 | 大小 | 说明 |
|------|---------|------|------|
| 操作系统 | OpenWRT | - | 路由器定制Linux |
| ASR | sherpa-onnx Zipformer | 25MB | 中文离线流式识别 |
| KWS | sherpa-onnx Transducer | 5MB | 唤醒词"小T小T"，常驻运行 |
| NLU | CNN+LSTM (ONNX) | 11MB（INT8 目标 3MB） | 16 意图 + 37 槽位 |
| 语音反馈 | 预录制音频拼接 | 0MB运行时 | Flash存储 |
| 设备控制 | GPIO / USB-TTL / ESP32中继 | - | 取决于部署阶段 |
| 通信 | WebSocket + UBus IPC | - | 外部设备 + 路由进程通信 |
