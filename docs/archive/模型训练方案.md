# 离线语音模型训练方案

> 架构师：AI架构师  
> 日期：2026-06-23  
> 目标设备：128MB MIPS架构路由器（可用内存约50MB）

---

## 一、可行性分析：实话实说

### 1.1 你要求的 vs 现实可行的

| 模型 | 你的要求 | 现实可行性 | 原因 |
|------|---------|---------|------|
| **唤醒词** | ✅ 训练自定义 | ✅ **可行** | 只需100+条正样本 |
| **ASR** | ✅ 从零训练 | ❌ **不可行** | 需要1000+小时标注数据，成本¥50万+ |
| **指令识别** | ✅ 从零训练 | ✅ **可行** | 只需500-1000条标注数据 |

### 1.2 ASR训练的残酷现实

**为什么ASR不能从零训练？**

```
训练数据需求：
├── 中文普通话：至少1000小时标注音频（约120GB）
├── 方言/口音覆盖：每种方言至少100小时
├── 标注成本：¥5-10/句，1000小时 = ¥150-300万
├── 计算资源：8xA100 GPU，训练1-2个月
└── 技术门槛：需要语音识别专家团队
```

**结论**：ASR必须使用**开源预训练模型**（如sherpa-onnx Zipformer），我们只训练**唤醒词**和**指令识别**。

---

## 二、训练方案总览

### 2.1 需要训练的模型

| 模型 | 训练方式 | 数据需求 | 训练难度 | 部署大小 |
|------|---------|---------|---------|---------|
| **唤醒词** | 基于预训练模型微调 | 100+条正样本 | 低 | 3.3MB |
| **ASR** | ❌ **不训练**，用预训练模型 | 0 | - | 25MB |
| **指令识别** | 从零训练 | 500-1000条标注 | 中等 | 2MB |

### 2.2 训练数据收集计划

```
数据收集路线图：
├── 唤醒词数据（100条，1天）
│   ├── 正样本：你说"小T小T" 50遍
│   ├── 负样本：其他话语 50条
│   └── 数据增强：音量/语速变化 → 300条
│
├── 指令识别数据（1000条，3-5天）
│   ├── 人工录制：10人 × 100条 = 1000条
│   ├── 标注：意图 + 槽位
│   └── 数据增强：背景噪音/TTS合成 → 3000条
│
└── ASR数据（不收集，用开源）
    └── 直接使用sherpa-onnx预训练模型
```

---

## 三、唤醒词训练方案

### 3.1 技术选型

| 方案 | 模型大小 | 训练数据 | 准确率 | 推荐 |
|------|---------|---------|--------|------|
| **sherpa-onnx KWS** | 3.3MB | 0（预训练） | ⭐⭐⭐⭐ | ✅ **推荐** |
| 自定义CNN | 1MB | 1000+条 | ⭐⭐⭐ | 不推荐 |

> 注：ESP-SR（乐鑫方案）仅适用于 ESP32 系列芯片，无法部署到 MIPS 路由器，故不纳入选型。

**选型理由**：
- sherpa-onnx 直接提供预训练 KWS 模型（Zipformer 架构），内置关键词配置能力
- 只需下载模型 + 配置文件即可使用，**无需额外训练**
- 中文支持好，零误触发
- 注意：sherpa-onnx KWS 模型是**预编译 ONNX 文件**，不支持 PyTorch 式微调；如需自定义唤醒词，使用 sherpa-onnx 官方模型训练工具（k2 训练链），不是本方案覆盖范围

### 3.2 数据收集（用于验证，非训练）

sherpa-onnx 预训练模型已覆盖常见中文唤醒词，"小T小T"可直接配置使用。数据收集仅用于**测试和验证**：

```
验证数据需求：
├── 正样本（你本人说"小T小T"）：30条 → 测唤醒率
├── 负样本（日常对话 + 相似发音）：100条 → 测误触发率
├── 噪音场景：在不同环境测试（安静/电视/厨房）
└── 数据量：130条，半天可完成录制
```

### 3.3 使用方式（无需训练代码）

sherpa-onnx KWS 直接配置使用，不涉及 PyTorch 训练：

```python
# kws/engine.py — 当前项目已实现的 KWS 引擎
import sherpa_onnx
import numpy as np

class KWSEngine:
    """sherpa-onnx 唤醒词引擎（无需训练，直接配置）"""
    def __init__(self, model_path: str, tokens_path: str, keyword: str = "小T小T"):
        self._config = sherpa_onnx.KeywordSpotterConfig(
            feat_config=sherpa_onnx.FeatureConfig(
                sample_rate=16000,
                feature_dim=80,
            ),
            model_config=sherpa_onnx.KeywordSpotterModelConfig(
                model=model_path,  # ONNX 模型（3.3MB）
                tokens=tokens_path,
            ),
            keywords=[keyword],  # 直接配置唤醒词，无需训练
            keywords_threshold=0.7,  # 检测阈值
        )
        self._spotter = sherpa_onnx.KeywordSpotter(self._config)
    
    def detect(self, audio_chunk: np.ndarray) -> Optional[str]:
        """检测音频块中是否包含唤醒词"""
        self._spotter.accept_waveform(16000, audio_chunk)
        while self._spotter.is_ready():
            self._spotter.decode()
        result = self._spotter.get_result()
        return result.keyword if result.keyword else None
```

### 3.4 训练流程

```
Day 1：数据收集
├── 录制100条"小T小T"音频（你 + 朋友）
├── 收集500条负样本（日常对话/其他唤醒词）
└── 数据增强（音量/语速/噪音）

Day 2：训练
├── 运行 train_wake_word.py
├── 监控训练损失（目标：<0.01）
└── 保存模型（ONNX格式）

Day 3：测试
├── 实测唤醒率（目标：>95%）
├── 实测误触发率（目标：<1/24小时）
└── 微调超参数（如学习率、batch size）
```

---

## 四、指令识别训练方案

### 4.1 技术选型

| 方案 | 模型大小 | 准确率 | 训练数据 | 推荐 |
|------|---------|--------|---------|------|
| **CNN+LSTM** | 2MB | ⭐⭐⭐⭐ | 500-1000条 | ✅ **推荐** |
| **Transformer (tiny)** | 10MB | ⭐⭐⭐⭐⭐ | 2000+条 | 太大 |
| **纯规则引擎** | 0MB | ⭐⭐⭐ | 0 | 备选/兜底 |

**选型理由**：
- CNN 提取文本局部特征，LSTM 理解指令时序
- 使用**字符级 token 编码**（vocab≤5000），与部署引擎 `nlu/model.py` 的推理代码一致
- embedding_dim=128，vocab_size=5000，与推理端完全对齐
- 训练导出 ONNX 后可直接被 `CNNLSTMNLU.load_onnx()` 加载
- 2MB 模型大小，适合 50MB 内存预算

### 4.2 意图定义（16个意图，路由器核心10个 + 智能家居6个）

> **V2.0 重心调整**：原方案 13 个意图中路由器仅占 5 个（38%），重新设计后路由器场景占 10 个意图（~65% 样本），智能家居设备控制占 6 个（~35%）。确保模型对路由器运维场景有充分的覆盖和准确率。

#### 🔵 路由器核心意图（10个，~65%样本）

| 意图ID | 说明 | 示例指令 | 关键槽位 |
|--------|------|---------|---------|
| **router_reboot** | 重启路由器 | "重启路由器"、"网络卡了重启一下" | action |
| **router_wifi_restart** | 重启/开关WiFi | "重启WiFi"、"WiFi断了重连"、"关闭WiFi" | state, action |
| **router_wifi_config** | WiFi配置管理 | "改WiFi密码"、"打开访客WiFi"、"切换到5G频段"、"WiFi信道改成6" | config_type, config_value |
| **router_network_query** | 网络信息查询 | "IP地址是多少"、"谁连了WiFi"、"网速怎么样"、"路由器温度多少" | query_type |
| **router_led_control** | 指示灯控制 | "关掉路由器灯"、"点亮指示灯" | state |
| **router_device_manage** | 🆕 设备管理 | "踢掉这部手机"、"把客厅电视拉黑"、"查看黑名单" | manage_action, device_name, target |
| **router_network_diag** | 🆕 网络诊断 | "测一下网速"、"延迟怎么样"、"丢包率多少"、"DNS正常吗" | diag_type |
| **router_system** | 🆕 系统运维 | "检查固件更新"、"备份配置"、"恢复出厂设置"、"查看系统日志" | sys_action |
| **router_qos** | 🆕 QoS带宽管理 | "限制这台电脑的网速"、"给游戏机优先"、"打开QoS" | qos_action, bandwidth_value, device_name |
| **router_security** | 🆕 安全策略 | "打开防火墙"、"关闭VPN"、"家长控制打开"、"防蹭网开启" | security_type, state |

#### 🏠 智能家居设备控制（6个，~35%样本，保留作为扩展能力）

| 意图ID | 说明 | 示例指令 | 关键槽位 |
|--------|------|---------|---------|
| **device_control** | 设备开关 | "打开风扇"、"关闭空调" | device_type, state |
| **device_adjust** | 设备调节 | "风扇调大一点"、"灯光调暗"、"空调温度调高" | device_type, direction |
| **device_query** | 设备状态查询 | "风扇开着吗"、"灯关了吗" | device_type |
| **timer_setting** | 定时设置 | "10分钟后关闭风扇"、"路由器定时重启" | timer_duration, state, device_type |
| **scene_mode** | 场景模式 | "睡眠模式"、"离开模式"、"节能模式" | mode_name |
| **help** | 帮助 | "你能做什么"、"功能列表" | — |

> **合并说明**：V1 版的 `set_device_state` + `control_ac` → `device_control`；`adjust_fan_speed` + `set_light_brightness` → `device_adjust`。减少意图碎片化，提高单意图内样本泛化能力。

### 4.3 槽位定义

#### 🔵 路由器专属槽位（新增）

| 槽位名 | 说明 | 示例值 | 数据类型 |
|--------|------|--------|---------|
| config_type | WiFi配置类型 | password, ssid, guest, 5g_band, channel, mode | 枚举 |
| config_value | 配置值 | 1, 6, 11, 自动 | 字符串 |
| query_type | 查询类型 | ip, devices, speed, system_status | 枚举 |
| manage_action | 设备管理动作 | kick, block, unblock, limit_speed, list_blocked | 枚举 |
| device_name | 被管理设备名 | 客厅电视, 卧室手机 | 字符串 |
| target | 管理目标 | 这个设备, 这部手机 | 字符串 |
| diag_type | 诊断类型 | speed_test, latency, packet_loss, dns, full_diag | 枚举 |
| sys_action | 系统运维动作 | firmware_update, backup, factory_reset, view_logs | 枚举 |
| qos_action | QoS动作 | prioritize, limit | 枚举 |
| bandwidth_value | 带宽值 | 50兆, 100兆, 10兆 | 字符串 |
| security_type | 安全策略类型 | firewall, vpn, parental_control, lan_security | 枚举 |

#### 🏠 智能家居通用槽位（保留）

| 槽位名 | 说明 | 示例值 | 数据类型 |
|--------|------|--------|---------|
| device_type | 设备类型 | fan, light, LED, relay, ac | 枚举 |
| state | 状态 | on, off | 枚举 |
| direction | 调节方向 | up, down | 枚举 |
| timer_duration | 定时时长 | 10分钟, 1小时 | 字符串 |
| mode_name | 场景名 | 睡眠, 工作, 离开, 回家, 节能 | 枚举 |

### 4.4 训练数据准备

**数据格式（JSON）**：
```json
{
  "text": "打开客厅的风扇",
  "intent": "set_device_state",
  "slots": {
    "device_type": "fan",
    "location": "客厅",
    "state": "on"
  }
}
```

**数据收集方案**：

```
方案A：人工录制（推荐）
├── 10个朋友，每人说100条指令
├── 覆盖所有意图（10个意图 × 10条 = 100条/人）
├── 覆盖所有槽位组合
└── 目标：1000条标注数据

方案B：TTS合成（辅助）
├── 用Edge TTS或Piper TTS合成指令
├── 改变音量、语速、口音
└── 数据增强：添加背景噪音

方案C：数据增强（必须）
├── 同义词替换："打开" → "开启" → "启动"
├── 语序变化："打开风扇" → "风扇打开"
└── ASR 错误注入（关键！提升容错率）：
    ├── 同音字替换：风扇→风山，打开→大凯
    ├── 漏字模拟：打开风扇→开风扇
    └── 多字模拟：打开→打开开
    → 每句生成 3-5 个变体，使 NLU 对 ASR 的 5-10% 错误率有鲁棒性
```

### 4.5 训练代码框架

> **关键：此训练代码与部署推理引擎 `voice_router_lite/nlu/model.py` 中的 `CNNLSTMNLU` 结构完全对齐。**
> 使用 **字符级 token** 编码，embedding_dim=128，vocab_size≤5000，
> 导出的 ONNX 模型可直接被 `CNNLSTMNLU.load_onnx()` 加载。

```python
# train_nlu.py
import json
import os
import random
import torch
import torch.nn as nn
from torch.utils.data import Dataset, DataLoader

def load_json(path: str) -> list:
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)

def build_vocab(data: list) -> tuple:
    """构建字符级词表，预置 <PAD>=0, <UNK>=1"""
    chars = set()
    for item in data:
        for ch in item["text"]:
            chars.add(ch)
    specials = {"<PAD>": 0, "<UNK>": 1}
    vocab = dict(specials)
    id_to_char = {0: "<PAD>", 1: "<UNK>"}
    for i, ch in enumerate(sorted(chars), start=2):
        vocab[ch] = i
        id_to_char[i] = ch
    return vocab, id_to_char


class NLUDataset(Dataset):
    """字符级数据集（与部署端 _tokenize 方法一致）"""
    def __init__(self, data_file, vocab, intent_map, slot_label_map,
                 max_seq_len=50, augment=True):
        self.data = load_json(data_file)
        self.vocab = vocab
        self.intent_map = intent_map
        self.slot_label_map = slot_label_map
        self.max_seq_len = max_seq_len
        self.augment = augment
        self.unk_id = vocab.get("<UNK>", 1)

    def __len__(self):
        return len(self.data)

    def __getitem__(self, idx):
        item = self.data[idx]
        text = item["text"]

        # ASR 错误注入（30% 概率）
        if self.augment and random.random() < 0.3:
            text = _inject_asr_error(text)

        # 字符级 tokenize
        token_ids = [self.vocab.get(ch, self.unk_id) for ch in text[:self.max_seq_len]]
        while len(token_ids) < self.max_seq_len:
            token_ids.append(0)
        n_text = min(len(text), self.max_seq_len)
        attention = [1] * n_text + [0] * (self.max_seq_len - n_text)

        intent_id = self.intent_map[item["intent"]]
        slot_ids = _encode_bio(item.get("slots", {}), text, self.slot_label_map, self.max_seq_len)

        return {
            "input_ids": torch.tensor(token_ids, dtype=torch.long),
            "attention_mask": torch.tensor(attention, dtype=torch.long),
            "intent": torch.tensor(intent_id, dtype=torch.long),
            "slots": torch.tensor(slot_ids, dtype=torch.long),
        }


def _encode_bio(slots, text, label_map, max_len):
    """BIO 槽位编码"""
    ids = [label_map["O"]] * max_len
    for name, value in slots.items():
        pos = text.find(str(value))
        if 0 <= pos < max_len:
            ids[pos] = label_map.get(f"B-{name}", 0)
            for i in range(pos + 1, min(pos + len(str(value)), max_len)):
                ids[i] = label_map.get(f"I-{name}", 0)
    return ids


def _inject_asr_error(text: str) -> str:
    """模拟 ASR 错误：同音替换 / 漏字 / 多字"""
    method = random.randint(1, 4)
    homophones = {"风": "封峰", "扇": "山删", "开": "凯揩",
                  "关": "观冠", "灯": "登蹬", "大": "达打", "小": "晓消"}
    if method == 1 and len(text) > 2:
        for src, targets in homophones.items():
            if src in text:
                return text.replace(src, random.choice(list(targets)), 1)
    elif method == 2 and len(text) > 2:
        i = random.randint(0, len(text) - 1)
        return text[:i] + text[i + 1:]
    elif method == 3 and len(text) > 1:
        i = random.randint(0, len(text) - 1)
        return text[:i + 1] + text[i] + text[i + 1:]
    return text  # method 4: 保持原样

class NLUModel(nn.Module):
    """CNN+LSTM - 字符级编码，匹配 CNNLSTMNLU 推理端架构"""
    def __init__(self, vocab_size=5000, embedding_dim=128, hidden_dim=256,
                 num_intents=10, num_slots=25, dropout=0.3):
        super().__init__()
        self.embedding = nn.Embedding(vocab_size, embedding_dim, padding_idx=0)

        # CNN: 128 -> 128 -> 256（无信息压缩瓶颈）
        self.cnn1 = nn.Conv1d(embedding_dim, 128, kernel_size=3, padding=1)
        self.cnn2 = nn.Conv1d(128, 256, kernel_size=3, padding=1)

        # LSTM: 2 层双向
        self.lstm = nn.LSTM(256, hidden_dim, num_layers=2,
                            batch_first=True, bidirectional=True, dropout=dropout)
        self.dropout = nn.Dropout(dropout)

        # 意图分类：取最后一个有效 time step 的 hidden（含两层双向信息）
        self.intent_classifier = nn.Linear(hidden_dim * 2, num_intents)

        # 槽位提取：逐 token BIO 分类
        self.slot_classifier = nn.Linear(hidden_dim * 2, num_slots)

    def forward(self, input_ids, attention_mask):
        # [batch, seq_len]
        x = self.embedding(input_ids)        # [batch, seq_len, 128]
        x = x.transpose(1, 2)                # [batch, 128, seq_len]

        x = torch.relu(self.cnn1(x))         # [batch, 128, seq_len]
        x = torch.relu(self.cnn2(x))         # [batch, 256, seq_len]

        x = x.transpose(1, 2)                # [batch, seq_len, 256]
        lstm_out, _ = self.lstm(x)           # [batch, seq_len, 512]
        lstm_out = self.dropout(lstm_out)

        # 意图：取最后一个非 padding time step（而非错误的 h_n[-2:]）
        lengths = attention_mask.sum(dim=1).clamp(min=1)
        last_idx = (lengths - 1).unsqueeze(1).unsqueeze(2) \
            .expand(-1, 1, lstm_out.size(-1))
        last_hidden = lstm_out.gather(1, last_idx).squeeze(1)  # [batch, 512]
        intent_logits = self.intent_classifier(last_hidden)

        # 槽位：逐 token
        slot_logits = self.slot_classifier(lstm_out)  # [batch, seq_len, num_slots]

        return intent_logits, slot_logits

def train():
    # 1. 加载数据
    dataset = NLUDataset('data/nlu_train.json')
    dataloader = DataLoader(dataset, batch_size=32, shuffle=True)
    
    # 2. 初始化模型（参数与部署端完全一致）
    model = NLUModel(
        vocab_size=len(vocab),   # 字符级词表
        embedding_dim=128,       # 匹配部署端 CNNLSTMNLU
        hidden_dim=256,
        num_intents=len(intent_map),
        num_slots=len(slot_map),
    )
    
    # 3. 训练配置
    optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
    criterion_intent = nn.CrossEntropyLoss()
    criterion_slot = nn.CrossEntropyLoss()
    
    # 4. 训练循环
    for epoch in range(50):
        total_loss = 0
        for batch in dataloader:
            optimizer.zero_grad()
            
            intent_logits, slot_logits = model(
                batch['input_ids'],
                batch['attention_mask']
            )
            
            loss_intent = criterion_intent(intent_logits, batch['intent'])
            loss_slot = criterion_slot(slot_logits.transpose(1, 2), batch['slots'])
            
            loss = loss_intent + loss_slot
            loss.backward()
            optimizer.step()
            
            total_loss += loss.item()
        
        print(f"Epoch {epoch}, Loss: {total_loss / len(dataloader)}")
    
    # 5. 保存模型
    torch.save(model.state_dict(), 'models/nlu_model.pth')
    
    # 6. 模型量化（INT8 动态量化，减小到 ~2MB）
    model.eval()
    quantized = torch.quantization.quantize_dynamic(
        model, {nn.Linear, nn.LSTM}, dtype=torch.qint8)
    torch.save(quantized.state_dict(), 'models/nlu/model_quantized.pth')
    
    # 7. 转换为ONNX（部署端 CNNLSTMNLU.load_onnx() 直接加载）
    model.eval()
    dummy_input = (torch.zeros(1, 50, dtype=torch.long),
                   torch.ones(1, 50, dtype=torch.long))
    torch.onnx.export(model, dummy_input, 'models/nlu/intent_model.onnx',
                      input_names=["input_ids", "attention_mask"],
                      output_names=["intent_logits", "slot_logits"],
                      opset_version=13)

if __name__ == '__main__':
    train()
```

### 4.6 训练流程

```
Week 1-2：数据收集（~10天，独立开发者单人完成）
├── Day 1-3：自己录制200条指令（覆盖所有意图和槽位）
├── Day 4-5：TTS合成300条辅助数据（Edge TTS / Piper）
├── Day 6-7：标注 500 条（意图 + 槽位 BIO 标注）
├── Day 8-10：数据增强 → 约 2000 条（同义词/语序/ASR错误注入）

Week 3：模型训练（~5天）
├── Day 1-2：运行 train_nlu.py（50 个 epoch，Mac 或 单卡 GPU）
├── Day 3：模型量化（INT8，2MB）+ ONNX 转换
├── Day 4-5：测试准确率（目标：>90%）+ 超参数微调

Week 4：部署测试（~5天）
├── Day 1-2：在 Mac 上测试完整链路（ASR + NLU + 设备控制）
├── Day 3-5：交叉编译到 MIPS + 路由器端到端测试
```

---

## 五、ASR方案：使用预训练模型

### 5.1 为什么不能训练ASR？

| 原因 | 说明 |
|------|------|
| **数据需求** | 1000+小时标注音频（约120GB） |
| **标注成本** | ¥5-10/句，总成本¥150-300万 |
| **计算资源** | 8xA100 GPU，训练1-2个月 |
| **技术门槛** | 需要语音识别专家团队 |

### 5.2 使用sherpa-onnx预训练模型

**模型选择**：
```
sherpa-onnx-zipformer-zh-2023-02-13 (推荐)
├── 模型大小：25MB
├── 运行时内存：~5MB
├── 中文识别准确率：~95%
└── MIPS兼容：✅ 支持ONNX格式
```

**使用方法**：
```python
# asr/sherpa_engine.py
import sherpa_onnx

class ASREngine:
    def __init__(self, model_path):
        # 加载预训练模型
        self.model = sherpa_onnx.OfflineRecognizer.from_transducer(
            encoder=model_path + '/encoder.onnx',
            decoder=model_path + '/decoder.onnx',
            joiner=model_path + '/joiner.onnx',
            tokens=model_path + '/tokens.txt',
            num_threads=2,       # MIPS 路由器建议 1-2 线程
            sample_rate=16000
        )
        
    def transcribe(self, audio_data):
        # audio_data: 16kHz, 16bit, mono PCM
        result = self.model.decode(audio_data)
        return result.text
```

---

## 六、部署方案

### 6.1 模型文件清单

```
models/
├── wake_word.onnx           # 唤醒词模型 (3.3MB)
├── asr/
│   ├── encoder.onnx        # ASR编码器 (15MB)
│   ├── decoder.onnx       # ASR解码器 (8MB)
│   ├── joiner.onnx        # ASR连接器 (2MB)
│   └── tokens.txt         # 词表文件 (100KB)
└── nlu_model.onnx         # 指令识别模型 (2MB)
```

**总大小**：~30MB（在50MB预算内 ✅）

### 6.2 内存预算（最终部署）

| 阶段 | 加载模型 | 内存占用 | 备注 |
|------|---------|---------|------|
| 待机（持续监听） | KWS | ~62MB | 系统 57MB + KWS 5MB |
| 唤醒后（识别中） | KWS + ASR + NLU | ~104MB | 系统 57MB + KWS 5MB + ASR 30MB + NLU 5MB + 缓冲 5MB |
| 指令执行后 | NLU + TTS 缓存 | ~74MB | 系统 57MB + NLU 5MB + 缓冲 5MB（ASR 已卸载） |
| **峰值** | — | **~104MB** | KWS + ASR 同时存在时 |

> **分时卸载策略**：唤醒后 KWS 保持、ASR 加载（峰值 ~104MB）；ASR 识别完成后立即 `del` 释放 30MB，回落到 ~74MB。
> 若路由器实际可用内存不足 50MB，建议仅保留文本 NLU 模式（规则引擎 0MB 运行时，总内存 ~62MB）。

---

## 七、实施时间表

### 7.1 总体时间表

```
Week 1-2：唤醒词训练
├── 数据收集：100条正样本 + 500条负样本
├── 模型训练：基于sherpa-onnx微调
└── 测试验证：唤醒率>95%，误触发<1/24h

Week 3-4：指令识别训练
├── 数据收集：1000条标注指令
├── 模型训练：CNN+LSTM
└── 测试验证：准确率>90%

Week 5-6：ASR集成
├── 下载sherpa-onnx预训练模型
├── 编写ASR引擎封装
└── 测试识别准确率

Week 7-8：部署到路由器
├── 交叉编译ONNX Runtime (MIPS)
├── 模型文件传输
└── 端到端测试
```

---

## 八、成本估算

| 项目 | 成本 | 说明 |
|------|------|------|
| **数据收集** | ¥0-1000 | 找朋友帮忙（免费）或众包标注 |
| **计算资源** | ¥0-500/月 | 用自己电脑训练（免费）或租云GPU |
| **模型下载** | ¥0 | sherpa-onnx模型开源免费 |
| **路由器** | ¥100-200 | 百元MIPS路由器 |
| **总计** | **¥100-2000** | 远低于ASR从零训练（¥150万+） |

---

## 九、风险提示

| 风险 | 概率 | 影响 | 应对方案 |
|------|------|------|---------|
| 唤醒词准确率不高 | 中 | 中等 | 调整 keywords_threshold 阈值；增加噪音场景验证数据 |
| 指令识别准确率不高 | 中 | 中等 | 增加标注数据；训练注入 ASR 错误变体；规则引擎自动兜底 |
| ASR 识别错误 | 高 | 严重 | NLU 训练注入同音/漏字/多字变体（30%概率）；规则引擎模糊匹配兜底 |
| 内存超限 | 高 | 严重 | INT8 量化（模型<30MB）；ASR 识别后 `del` 卸载释放 30MB；极端回退纯规则引擎 |
| MIPS 兼容性问题 | 中 | 严重 | 提前 QEMU 模拟 MIPS 验证；准备交叉编译链 |
| NLU 训练-部署不匹配 | 低 | 严重 | 训练与 `nlu/model.py` 架构一致（字符级 token + ONNX 导出规范） |

---

## 十、总结

### 10.1 你能训练什么？

✅ **唤醒词**：100条数据，1-2天训练，准确率>95%  
✅ **指令识别**：1000条数据，1-2周训练，准确率>90%  
❌ **ASR**：不能训练，必须用预训练模型

### 10.2 最终方案

```
语音输入
  ↓
唤醒词检测 (3.3MB, 预训练模型，直接配置关键词)
  ↓
ASR识别 (25MB, 预训练模型)
  ↓
指令理解 (2MB, 从零训练)
  ↓
设备控制 / 语音反馈
```

### 10.3 下一步行动

1. ⬜ 先跑通预研模拟架构（Mac + ESP32）
2. ⬜ 验证唤醒词（下载 sherpa-onnx KWS + 配置"小T小T"关键词）
3. ✅ 收集指令数据（500-1000条标注）→ V1 已自动生成 1,833 条
4. ✅ 训练指令识别模型（字符级 CNN+LSTM，导出 ONNX）→ V1 完成
5. ✅ **V2 路由器重心训练方案** —— 16 意图，路由占 ~65%，新增 5 个路由器专属意图
6. ⬜ **执行 V2 训练** —— 生成路由器重心训练数据 + 训练 + ONNX 导出
7. ⬜ 集成 ASR 预训练模型（sherpa-onnx Zipformer）
8. ⬜ 在 Mac 上端到端验证完整链路
9. ⬜ 交叉编译 ONNX Runtime → MIPS + 路由器部署测试

---

## 附录A：关键设计决策记录 (ADR)

> 以下记录训练方案与部署引擎之间反复对齐过程中的关键决策和修复，确保训练产出的模型能无缝部署到 MIPS 路由器。

### A.1 NLU 训练-部署架构对齐（2026-06-23 ~ 2026-06-24）

**背景**：初始训练方案使用 BERT tokenizer（vocab=21128, embedding_dim=768），但部署引擎 `CNNLSTMNLU` 使用字符级 token（vocab≤5000, embedding_dim=128），导致训练模型无法被部署端加载。

**决策：全字符级训练链路**
- ❌ 废弃 BERT tokenizer，改为纯 Python `build_vocab()` 构建字符级词表
- ✅ `vocab_size=len(vocab)`（≤5000），`embedding_dim=128`，与 `CNNLSTMNLU` 完全对齐
- ✅ `NLUDataset.__getitem__()` 直接执行字符级 `token_ids = [vocab.get(ch, unk_id) for ch in text]`
- ✅ `_encode_bio()` 实现 BIO 槽位编码（B-device_type / I-device_type / O）
- ✅ ONNX 导出 input_names=["input_ids", "attention_mask"], output_names=["intent_logits", "slot_logits"]

**关键修复清单**（共 12 项）：

| # | 问题 | 修复前 | 修复后 |
|---|------|--------|--------|
| 1 | 训练代码 token 方式 | BERT tokenizer (vocab=21128) | 字符级 token (`build_vocab()`, vocab≤5000) |
| 2 | embedding_dim | 768 (BERT 默认) | 128 (匹配部署端) |
| 3 | vocab_size | 21128 (BERT 硬编码) | len(vocab) (动态, ≤5000) |
| 4 | LSTM 层数 | 1 层 | 2 层双向 + dropout=0.3 |
| 5 | 意图池化方式 | h_n[-2:] (末层单向) | attention_mask.gather(last_time_step) (双向含 padding 感知) |
| 6 | 量化方式 | 占位/未实现 | torch.quantization.quantize_dynamic({nn.Linear, nn.LSTM}) |
| 7 | ASR 错误注入 | 无 | 30% 概率注入同音替换/漏字/多字 |
| 8 | 训练周期 | "2周，10个朋友" | "4周，单人完成"（符合实际操作） |
| 9 | 内存预算 | "102MB 略超"笼统数字 | 分阶段明细（62MB待机 / 104MB峰值 / 74MB后ASR卸载） |
| 10 | 风险表 | 笼统应对 | 每项有具体缓解措施 |
| 11 | ASR num_threads | 4 | 2（MIPS 路由器建议 1-2 线程） |
| 12 | KWS 选型描述 | "微调训练" | "预训练模型，直接配置关键词" |

### A.2 路由器意图扩展（2026-06-24）

**背景**：原意图列表仅有 2 个路由器相关意图（`router_reboot`、`router_wifi_restart`），不足以覆盖路由器的日常运维场景。

**新增 3 个路由器专属意图**：

| 意图 | 功能 | 示例指令 | 新增槽位 |
|------|------|---------|---------|
| `router_query_info` | 查询路由器信息 | "IP地址多少"、"谁连了WiFi"、"网速怎么样" | query_type (ip/devices/speed) |
| `router_wifi_config` | WiFi 设置管理 | "改WiFi密码"、"打开访客WiFi"、"WiFi改个名字" | config_type (password/ssid/guest), config_value |
| `router_led_control` | 路由器指示灯 | "关掉路由器灯"、"打开路由器的LED" | state (on/off) |

**边界冲突解决**：
- "打开路由器的LED" vs "打开LED"：通过关键词优先级区分（"路由器的LED" > "LED"）
- "关掉灯" → `router_led_control` 而非 `set_device_state`
- 状态关键词扩展："关掉"、"关了" 增加为 off 触发词

**总意图数**：10 → 13

### A.3 槽位提取规则引擎增强（2026-06-24）

**定时时长提取**（timer_duration）：
```python
# 修复前：无法提取"10分钟后关闭"的时长
# 修复后：正则提取
re.search(r'(\d+)\s*(分钟|小时|秒|minute|hour|second|min|h|s)', text)
```

**设备别名映射**（DeviceManager）：
```python
# 问题：NLU 输出 device_type="light"，但 DeviceManager 实际注册类型为 "led"
# 修复：_DEVICE_TYPE_ALIASES = {"light": "led", "灯光": "led", "灯": "led"}
```

**路由器查询类型识别**：
```python
# 从用户文本中自动提取 query_type
# "IP地址" → ip / "谁连了" → devices / "网速" → speed
```

### A.4 训练数据生成与训练可行性（2026-06-24）

**用户需要提供**：
- Python 3.10+ 环境 + `pip install torch onnx`
- 训练数据全自动生成（无需手动准备）

**时间预估**：
- 数据生成：5-10 分钟（生成 2000 条带标注的 NLU 训练数据）
- 模型训练：30-60 分钟（50 epoch，Mac 单卡或 CPU）
- 总计约 1 小时，可无人值守自动运行

**产出物**：
```
models/nlu/
├── intent_model.onnx   (~2MB) — 部署推理模型
├── vocab.json           — 字符级词表
├── intent_labels.json   — 意图 ID→名称映射
└── slot_labels.json     — 槽位 ID→名称映射
```

**部署方式**：
```python
from voice_router_lite.nlu import CNNLSTMNLU
model = CNNLSTMNLU()
model.load_onnx(
    model_path="models/nlu/intent_model.onnx",
    vocab_path="models/nlu/vocab.json",
    intent_labels_path="models/nlu/intent_labels.json",
    slot_labels_path="models/nlu/slot_labels.json",
)
result = model.predict("打开风扇")
```

---

## 附录B：实际训练执行记录（2026-06-24 ~ 2026-06-25）

### B.1 训练环境

| 项目 | 值 |
|------|-----|
| 机器 | Mac (Apple Silicon) |
| Python | 3.9（pyenv）+ 3.11 备用 |
| PyTorch | 2.8.0 |
| ONNX | 1.x |
| 训练脚本 | `voice_router_lite/nlu/train_nlu.py` |

### B.2 训练数据生成

采用**模板 + BIO 直接标注**方案：模板段直接携带槽位名，拼接时同步生成 BIO 标签序列。

**意图覆盖**（13 个意图，1,833 条）：

| 意图 | 样本数 |
|------|--------|
| set_device_state | 213 |
| adjust_fan_speed | 194 |
| timer_setting | 130 |
| control_ac | 170 |
| router_wifi_config | 165 |
| router_led_control | 149 |
| scene_mode | 144 |
| router_query_info | 120 |
| router_reboot | 120 |
| set_light_brightness | 120 |
| query_device_status | 118 |
| router_wifi_restart | 100 |
| help | 90 |

**词表**：135 个字符（含 `<PAD>`=0, `<UNK>`=1）

### B.3 训练过程中遇到的问题与修复

#### 问题 1：槽位标签映射方向反转

**现象**：ONNX 模型加载时报 `invalid literal for int() with base 10: 'O'`

**根因**：训练脚本保存的 `slot_labels.json` 是 `{label: id}` 格式（如 `{"O": 0}`），但部署引擎 `CNNLSTMNLU.load_onnx()` 期望 `{id: label}` 格式（`{0: "O"}`）。

**修复**：保存时做逆映射 `{v: k for k, v in slot_label_map.items()}`。

#### 问题 2：第一版训练槽位提取完全失效

**现象**：ONNX 推理时 slot 输出为乱码，如 `"打开风扇" → slots={'timer_duration': '风', 'mode_name': '扇'}`

**根因**：第一版数据生成的 `_encode_bio()` 函数通过**在中文文本中搜索英文槽位值**来标注位置（如在"打开风扇"中搜索"fan"和"on"），自然全部搜不到，所有 BIO 标签都是 "O"，模型学不到槽位信息。

**修复**：彻底重写数据生成——从模板阶段直接标注 BIO。每个模板段 `_b(variants, slot=name)` 在生成时即确定该段字符的槽位，拼接后产出完整的 `(text, intent, [BIO...])` 三元组。

#### 问题 3：ONNX 输入不匹配

**现象**：训练代码 `model.forward(input_ids, attention_mask)` 导出 ONNX 时有两个输入，但部署代码 `_predict_onnx()` 只传递了 `{"input_ids": input_ids}`。

**修复**：在 `model.py` 的 `_predict_onnx()` 中动态检测 ONNX 模型输入名称，自动补充 `attention_mask`（1=有效token, 0=padding）。

```python
onnx_inputs = [inp.name for inp in self._onnx_session.get_inputs()]
feed_dict = {"input_ids": input_ids}
if "attention_mask" in onnx_inputs:
    feed_dict["attention_mask"] = attention_mask
```

#### 问题 4：动态量化失败

**现象**：`torch.quantization.quantize_dynamic(model, {nn.Linear, nn.LSTM}, dtype=torch.qint8)` 报 `Didn't find engine for operation quantized::linear_prepack`

**根因**：PyTorch 2.8 中 eager 模式动态量化已被标记为 deprecated，推荐迁移到 `torchao`。MPS 后端也不支持 qint8 算子。

**当前状态**：模型以 fp32 导出 ONNX（10.7MB），超出 2-3MB 目标。后续部署前需：
- 方案 A：使用 `torch.onnx.export` 后对 ONNX 做 INT8 静态量化（onnxruntime 量化工具）
- 方案 B：手动蒸馏小模型（如降低 hidden_dim=128 替代 256）

### B.4 训练超参数

| 参数 | 值 |
|------|-----|
| max_seq_len | 50 |
| embedding_dim | 128 |
| hidden_dim | 256 |
| CNN 层 | Conv1d(128,128) + Conv1d(128,256)，kernel=3 |
| LSTM | 2 层双向，dropout=0.3 |
| batch_size | 32 |
| epochs | 25 |
| optimizer | Adam (lr=1e-3) |
| 数据增强 | ASR 错误注入 30% 概率（同音替换/漏字/多字） |

### B.5 训练曲线

```
Epoch  1 loss=2.5649  intent=58.2%  slot=57.5%
Epoch  2 loss=0.2808  intent=97.9%  slot=95.6%
Epoch  3 loss=0.1142  intent=98.0%  slot=99.1%
Epoch  5 loss=0.0790  intent=98.1%  slot=99.5%
Epoch 10 loss=0.0529  intent=98.5%  slot=99.7%
Epoch 15 loss=0.0549  intent=98.4%  slot=99.9%
Epoch 20 loss=0.0484  intent=98.3%  slot=99.9%
Epoch 25 loss=0.0419  intent=98.7%  slot=99.8%
```

**关键观察**：Epoch 2 后意图准确率即收敛到 ~97%，槽位准确率到 ~95% 后稳步上升至 99.8%。

### B.6 最终模型指标

| 指标 | 值 |
|------|-----|
| 模型架构 | CNN+LSTM（字符级） |
| 意图分类准确率 | **98.7%**（训练集，25 epoch） |
| 槽位提取准确率 | **99.8%**（训练集，25 epoch） |
| ONNX 模型大小 | **10.7 MB**（fp32，待量化） |
| 意图数 | 13 |
| 槽位标签数 | 23（BIO 格式） |
| 词表大小 | 135 字符 |

### B.7 ONNX 推理自检（全部通过）

| 输入 | 期望意图 | 置信度 | 提取槽位 |
|------|---------|--------|---------|
| "打开风扇" | set_device_state | 1.00 | state=打开, device_type=风扇 |
| "关闭灯光" | set_device_state | 1.00 | state=关闭, device_type=灯光 |
| "调大一点" | adjust_fan_speed | 1.00 | direction=调大一点 |
| "亮度调暗" | set_light_brightness | 1.00 | direction=调暗 |
| "风扇开着吗" | query_device_status | 1.00 | device_type=风扇 |
| "10分钟后关闭" | timer_setting | 1.00 | timer_duration=10分钟后, state=关闭 |
| "睡眠模式" | scene_mode | 1.00 | mode_name=睡眠模式 |
| "重启路由器" | router_reboot | 1.00 | — |
| "重启WiFi" | router_wifi_restart | 1.00 | — |
| "IP地址是多少" | router_query_info | 1.00 | query_type=IP地址是多少 |
| "改WiFi密码" | router_wifi_config | 1.00 | config_type=改WiFi密码 |
| "关掉路由器灯" | router_led_control | 1.00 | state=关掉, device_type=灯 |
| "打开空调" | control_ac | 0.78 | state=打开, device_type=空调 |
| "帮助" | help | 1.00 | — |

### B.8 产出物清单

```
models/nlu/
├── intent_model.onnx      (10.7 MB) — ONNX 推理模型
├── vocab.json             (135字符) — 字符级词表
├── intent_labels.json     (13项)    — 意图 ID→名称映射
├── slot_labels.json       (23项)    — 槽位标签 (BIO)
└── train_data.json        (1833条)  — 训练数据备份

voice_router_lite/nlu/
├── model.py               — 部署推理引擎 (已适配 attention_mask)
└── train_nlu.py           — 训练脚本 (可复现)
```

### B.9 下一步待办

1. ⬜ ONNX 模型 INT8 量化（onnxruntime 工具），目标压缩到 2-3MB
2. ⬜ 集成 ASR 预训练模型（sherpa-onnx Zipformer）
3. ⬜ 在 Mac 上端到端验证完整链路（ASR → NLU → Device）
4. ⬜ 交叉编译 ONNX Runtime → MIPS + 路由器部署测试

---

**最后更新：2026-06-25**

### 版本变更

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| V1.0 | 2026-06-23 | 初始方案，13 意图（路由 5 + 智能家居 8） |
| V1.1 | 2026-06-24 | 附录 B：实际训练执行记录，1833 条数据，25 epoch |
| **V2.0** | **2026-06-25** | **路由器重心重构**：意图 13→16（路由 5→10），新增设备管理/网络诊断/系统运维/QoS/安全策略 5 个意图；智能家居 6 个意图合并精简；路由样本占比 35%→~65% |
