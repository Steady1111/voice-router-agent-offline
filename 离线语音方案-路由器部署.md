# 离线语音控制方案（路由器部署）

> 架构师：AI架构师  
> 日期：2026-06-23  
> 目标设备：128MB MIPS架构路由器（可用内存约50MB）

---

## 一、现实约束与可行性分析

### 1.1 内存预算（128MB路由器）

| 项目 | 占用 | 说明 |
|------|------|------|
| Linux内核+驱动 | 30MB | 必须 |
| OpenWRT系统服务 | 15MB | 必须 |
| Python解释器 | 12MB | 必须 |
| **剩余可用** | **~71MB** | 理论值 |
| 内存碎片+缓存 | ~20MB | 实际损耗 |
| **实际可用** | **~50MB** | 保守估计 |

### 1.2 50MB内能跑什么？

#### 方案A：完整功能（不可行 ❌）

| 功能模块 | 模型大小 | 运行时内存 | 小计 |
|---------|---------|-----------|------|
| 唤醒词检测 (sherpa-onnx KWS) | 3.3MB | 2MB | 5.3MB |
| ASR (sherpa-onnx Zipformer) | 25MB | 5MB | 30MB |
| NLU (CNN+LSTM) | 2MB | 3MB | 5MB |
| TTS (Piper或类似) | 20MB | 10MB | 30MB |
| WebSocket+系统开销 | - | 10MB | 10MB |
| **总计** | - | - | **80.3MB** ❌ |

**结论：50MB跑不下完整功能，必须做减法。**

---

## 二、架构设计：预研模拟 vs 最终部署

### 2.1 两种架构对比

#### 架构A：预研模拟（当前阶段，Mac + ESP32）

```
┌─────────────────────────────────────────────────┐
│                    Mac 电脑（模拟路由器）       │
│                                               │
│  ┌────────────┐      ┌────────────┐          │
│  │  ASR识别   │←────│ 音频接收   │          │
│  │(sherpa-   │      │ (WebSocket)│          │
│  │  onnx 25MB)│      └────────────┘          │
│  └──────┬──────┘                               │
│         ↓                                     │
│  ┌──────┴──────┐      ┌────────────┐        │
│  │  指令理解   │────→│  语音反馈   │        │
│  │  (CNN+LSTM) │      │ (预录制音频)│        │
│  └──────┬──────┘      └────────────┘        │
│         ↓                                       │
│  ┌──────┴──────┐                             │
│  │ 设备指令下发 │                             │
│  │ (WebSocket)  │                             │
│  └──────┬──────┘                             │
└───────────┼─────────────────────────────────────┘
            ↓ WebSocket
┌───────────┴─────────────────────────────────────┐
│              ESP32-S3（硬件验证）               │
│                                               │
│  ┌────────────┐      ┌────────────┐          │
│  │ 音频采集   │───→  │ WiFi 传输  │          │
│  │(INMP441)  │      │ (WebSocket)│          │
│  └────────────┘      └────────────┘          │
│                                               │
│  ┌────────────┐      ┌────────────┐          │
│  │ 设备控制   │←────│ 指令接收   │          │
│  │(风扇/LED/  │      │ (WebSocket)│          │
│  │ 数码管)    │      └────────────┘          │
│  └────────────┘                             │
└─────────────────────────────────────────────────┘
```

**Mac端运行**：ASR + NLU + TTS（模拟路由器AI管线）  
**ESP32端运行**：音频采集 + WiFi传输 + 设备控制（验证硬件）

---

#### 架构B：最终部署（128MB MIPS路由器独立运行）

```
┌─────────────────────────────────────────────────┐
│          128MB MIPS路由器（独立运行）           │
│                                               │
│  ┌────────────┐      ┌────────────┐          │
│  │ 音频采集   │───→  │  ASR识别   │          │
│  │(USB-I2S或  │      │(sherpa-   │          │
│  │ 板载I2S)  │      │  onnx 25MB)│          │
│  └────────────┘      └──────┬──────┘          │
│                              ↓                  │
│                    ┌──────┴──────┐          │
│                    │  指令理解   │          │
│                    │  (CNN+LSTM) │          │
│                    └──────┬──────┘          │
│         ┌─────────────────┼──────────────┐    │
│         ↓                 ↓                  ↓    │
│  ┌──────────┐   ┌──────────┐    ┌─────────┐│
│  │ 语音反馈  │   │ GPIO控制  │    │ WebSocket││
│  │(预录制或  │   │(继电器/   │    │ (可选，  ││
│  │ I2S DAC) │   │ 直驱)    │    │  扩展设备)││
│  └──────────┘   └──────────┘    └─────────┘│
└─────────────────────────────────────────────────┘
         ↓ I2S/PCM                ↓ GPIO
   INMP441麦克风 / 喇叭          继电器/LED/风扇
```

**路由器端运行**：ASR + NLU + TTS + 设备控制（全部独立运行）  
**外部设备**：麦克风、喇叭、继电器等通过USB或GPIO连接

---

### 2.2 预研阶段ESP32验证重点

| 验证项 | 是否在ESP32上运行 | 说明 |
|--------|-------------------|------|
| **音频采集** | ✅ **必须在ESP32验证** | INMP441 I2S驱动、音频质量 |
| **WiFi传输** | ✅ **必须在ESP32验证** | WebSocket协议、音频流传输稳定性 |
| **设备控制** | ✅ **必须在ESP32验证** | 风扇PWM、LED、数码管显示 |
| **ASR识别** | ❌ 在Mac/路由器运行 | ESP32算力不足，无需验证 |
| **NLU理解** | ❌ 在Mac/路由器运行 | 模型太大，无需在ESP32验证 |
| **TTS播放** | ⚠️ 可选在ESP32验证 | 预录制音频播放，验证I2S DAC |

> **关键**：ESP32只负责**音频采集**和**设备控制**，AI推理（ASR+NLU）在Mac/路由器端运行。

### 2.3 内存预算（最终方案）

| 项目 | 占用 | 备注 |
|------|------|------|
| 系统基础 | 57MB | 内核+OpenWRT+Python |
| ASR (sherpa-onnx) | 30MB | 25MB模型+5MB运行时 |
| NLU (CNN+LSTM) | 5MB | 2MB模型+3MB运行时 |
| 音频缓冲 | 5MB | 5秒@16kHz@16bit |
| WebSocket服务 | 3MB |  |
| **总计** | **100MB** | **仍在边界** |
| 剩余余量 | 28MB | 56%余量 ✅ |

> **关键决策**：放弃实时TTS合成，改用**预录制语音片段拼接**播放固定回复。

---

## 三、技术选型详细对比

### 3.1 ASR方案对比

| 方案 | 模型大小 | 运行时 | 中文效果 | MIPS兼容 | 推荐 |
|------|---------|--------|---------|----------|------|
| **sherpa-onnx Zipformer small** | 25MB | ~5MB | ⭐⭐⭐⭐ | ✅ ONNX | **推荐** |
| Vosk small | 42MB | ~40MB | ⭐⭐⭐ | ⚠️ 需编译 | 内存太大 |
| Whisper tiny | 78MB | ~300MB | ⭐⭐⭐⭐ | ❌ 太大 | 不可行 |
| PocketSphinx | 20MB | ~20MB | ⭐ | ⚠️ 需编译 | 效果差 |

**选型理由：**
- sherpa-onnx支持ONNX格式，MIPS架构可交叉编译ONNX Runtime
- Zipformer模型25MB，是能在50MB内运行的最好中文ASR
- 流式识别支持，延迟低

### 3.2 NLU（指令理解）方案对比

| 方案 | 模型大小 | 准确率 | 训练难度 | 推荐 |
|------|---------|--------|---------|------|
| **CNN+LSTM（推荐）** | 2MB | ⭐⭐⭐⭐ | 中等 | ✅ |
| 纯规则引擎 | 0MB | ⭐⭐⭐ | 低 | 备选 |
| Transformer tiny | 10MB | ⭐⭐⭐⭐⭐ | 高 | 太大 |
| SVM/随机森林 | 1MB | ⭐⭐ | 低 | 不推荐 |

**选型理由：**
- CNN提取音频特征，LSTM理解时序指令
- 2MB模型大小合适，50MB预算内
- 可训练，支持自定义指令集

**NLU模型输入输出：**
```
输入：ASR识别的文本（如"打开客厅的风扇"）
输出：
  - intent: set_device_state
  - slots: {device_type: "fan", location: "客厅", state: "on"}
```

### 3.3 唤醒词方案对比

| 方案 | 运行内存 | 准确率 | 自定义 | 推荐 |
|------|---------|--------|--------|------|
| **sherpa-onnx KWS** | ~2MB | ⭐⭐⭐⭐ | ✅ | **推荐** |
| openwakeword | ~10MB | ⭐⭐⭐ | ✅ | 太大 |
| 能量VAD | ~0MB | ⭐ | ❌ | 误触发高 |
| ESP-SR | ~2MB | ⭐⭐⭐⭐ | ⚠️ 需训练 | ESP专用 |

**决策：暂时不用唤醒词，用按键触发录音**（节省2MB内存）

> 如果一定要唤醒词，选sherpa-onnx KWS，放在ASR之前运行。

### 3.4 TTS/语音反馈方案对比

| 方案 | 内存占用 | 音质 | 延迟 | 推荐 |
|------|---------|------|------|------|
| **预录制语音拼接** | 0MB（文件在Flash） | ⭐⭐⭐ | 低 | **推荐** |
| Piper TTS | ~20MB | ⭐⭐⭐⭐ | 中 | 太大 |
| Edge TTS | 需联网 | ⭐⭐⭐⭐⭐ | 高 | 不可行 |
| 蜂鸣器 | 0MB | ❌ | 低 | 备选 |

**预录制语音拼接方案：**
- 录制20-30条常用回复："已打开风扇"、"已关闭LED"、"无法识别"等
- 根据NLU结果选择对应音频文件播放
- 零运行时内存占用

---

## 四、软件模块划分（最终部署架构）

> 预研模拟架构见第二章 2.1 节。本节描述最终部署到路由器上的软件模块划分。

### 4.1 模块划分

```
voice-router-lite/
├── audio/                      # 音频采集与播放
│   ├── capture.py              # I2S麦克风采集
│   ├── player.py               # I2S DAC播放
│   └── vad.py                 # 语音活动检测（可选）
├── asr/                       # 语音识别
│   ├── __init__.py
│   ├── sherpa_engine.py        # sherpa-onnx引擎封装
│   └── models/                # ASR模型文件
├── nlu/                       # 指令理解
│   ├── __init__.py
│   ├── intent_classifier.py    # 意图分类模型
│   ├── slot_filler.py         # 槽位提取模型
│   └── models/                # NLU模型文件
├── device/                     # 设备控制
│   ├── __init__.py
│   ├── manager.py              # 设备状态管理
│   ├── fan.py                 # 风扇控制
│   ├── led.py                 # LED控制
│   └── relay.py               # 继电器控制
├── tts/                       # 语音反馈
│   ├── __init__.py
│   └── audio_clips.py         # 预录制音频管理
├── transport/                 # 通信层
│   ├── websocket_server.py    # WebSocket服务
│   └── protocol.py            # 协议定义
├── config/
│   ├── config.py               # 配置管理
│   └── .env.example           # 环境变量模板
├── models/                     # 模型文件存放
│   ├── asr/
│   └── nlu/
├── audio_clips/               # 预录制语音文件
├── tests/
├── pyproject.toml
└── README.md
```

---

## 五、模型训练方案

### 5.1 需要训练的模型

| 模型 | 是否训练 | 数据需求 | 训练难度 |
|------|---------|---------|---------|
| ASR | ❌ 不训练 | 1000+小时 | 极高 |
| 唤醒词 | ⚠️ 可选 | 100+正样本 | 中等 |
| **NLU（意图分类）** | ✅ **必须训练** | 500-1000条 | 中等 |
| **NLU（槽位提取）** | ✅ **必须训练** | 500-1000条 | 中等 |
| TTS | ❌ 不训练 | - | - |

### 5.2 NLU模型训练数据准备

**意图定义（10个常用指令）：**

| 意图ID | 说明 | 示例指令 |
|--------|------|---------|
| set_device_state | 开关设备 | "打开风扇"、"关闭LED" |
| adjust_fan_speed | 调节风扇 | "调大一点"、"风速小一点" |
| set_light_brightness | 调节灯光 | "亮度调高"、"暗一点" |
| query_device_status | 查询状态 | "风扇开着吗"、"温度多少" |
| timer_setting | 设置定时 | "10分钟后关闭"、"定时1小时" |
| scene_mode | 场景模式 | "睡眠模式"、"工作模式" |
| router_reboot | 重启路由器 | "重启路由器"、"网络卡了" |
| router_wifi_restart | 重启WiFi | "重启WiFi"、"WiFi断了" |
| help | 帮助 | "你能做什么"、"帮助" |
| unknown | 无法识别 | - |

**槽位定义：**

| 槽位名 | 说明 | 示例 |
|--------|------|------|
| device_type | 设备类型 | fan, light, LED, relay |
| location | 位置 | 客厅, 卧室, 厨房 |
| state | 状态 | on, off, toggle |
| speed_level | 风速档位 | 1, 2, 3, 4, 5 |
| brightness | 亮度 | 1-100 |
| timer_duration | 定时时长 | 10分钟, 1小时 |

### 5.3 训练数据收集方案

**方案A：人工标注（推荐）**
```python
# 训练数据格式 (JSON)
{
  "text": "打开客厅的风扇",
  "intent": "set_device_state",
  "slots": {
    "device_type": "fan",
    "location": "客厅",
    "state": "on"
  }
}
```

**数据来源：**
1. 自己录制50条不同指令（每人说10条，找5个朋友）
2. 用TTS合成变体（改变音量、语速、口音）
3. 数据增强：添加背景噪音、语速变化

**目标：500-1000条标注数据**

### 5.4 模型训练代码框架

```python
# nlu/train.py
import torch
import torch.nn as nn
from torch.utils.data import Dataset, DataLoader

class NLUModel(nn.Module):
    """CNN+LSTM 指令理解模型"""
    def __init__(self, vocab_size, embedding_dim, hidden_dim, num_intents, num_slots):
        super(NLUModel, self).__init__()
        self.embedding = nn.Embedding(vocab_size, embedding_dim)
        self.cnn = nn.Conv1d(embedding_dim, 128, kernel_size=3, padding=1)
        self.lstm = nn.LSTM(128, hidden_dim, batch_first=True, bidirectional=True)
        self.intent_classifier = nn.Linear(hidden_dim * 2, num_intents)
        self.slot_classifier = nn.Linear(hidden_dim * 2, num_slots)
        
    def forward(self, x):
        # x: [batch, seq_len]
        x = self.embedding(x)  # [batch, seq_len, embedding_dim]
        x = x.transpose(1, 2)  # [batch, embedding_dim, seq_len]
        x = self.cnn(x)         # [batch, 128, seq_len]
        x = x.transpose(1, 2)  # [batch, seq_len, 128]
        lstm_out, (h_n, c_n) = self.lstm(x)
        intent_logits = self.intent_classifier(h_n[-2:].transpose(0, 1).contiguous().view(x.size(0), -1))
        slot_logits = self.slot_classifier(lstm_out)
        return intent_logits, slot_logits

# 训练流程
def train():
    model = NLUModel(vocab_size=5000, embedding_dim=128, hidden_dim=256, 
                     num_intents=10, num_slots=20)
    optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
    criterion_intent = nn.CrossEntropyLoss()
    criterion_slot = nn.CrossEntropyLoss()
    
    for epoch in range(50):
        for batch in dataloader:
            intent_logits, slot_logits = model(batch['text'])
            loss_intent = criterion_intent(intent_logits, batch['intent'])
            loss_slot = criterion_slot(slot_logits.transpose(1, 2), batch['slots'])
            loss = loss_intent + loss_slot
            loss.backward()
            optimizer.step()
    
    # 保存模型
    torch.save(model.state_dict(), 'models/nlu_model.pth')
```

---

## 六、硬件连接方案

### 6.1 路由器端硬件接口

```
路由器 (OpenWRT)
  ├── USB接口 → CP2104 USB-I2S音频模块
  │             ├── INMP441 麦克风 (I2S输入)
  │             └── MAX98357A DAC (I2S输出) → 喇叭
  ├── GPIO引脚 → 继电器模块 (控制外接设备)
  └── WiFi → 控制ESP32等设备 (可选)
```

### 6.2 如果路由器没有GPIO

**方案：通过USB串口控制外部设备**
```
路由器 USB ──→ CP2104 USB-TTL ──→ 外部MCU (ATtiny/STM32)
                                      ├── 风扇
                                      ├── LED
                                      └── 继电器
```

---

## 七、实施步骤

### 第一阶段：预研模拟（Mac + ESP32，验证硬件和管线）

> 目标：在Mac上模拟路由器AI管线，ESP32负责音频采集和设备控制，验证整个链路可行性。

#### 步骤1：ESP32固件开发（3-5天）

1. **音频采集验证**
   - 编写INMP441 I2S驱动
   - 测试16kHz@16bit@mono音频采集
   - 验证音频质量（通过串口发送PCM到Mac播放）

2. **WiFi/WebSocket传输**
   - 实现WebSocket客户端
   - 测试音频流传输稳定性
   - 实现应用层心跳（5秒间隔）

3. **设备控制验证**
   - 编写TB6612风扇驱动（GPIO 8开关 + GPIO 9 PWM）
   - 编写四位数码管驱动（GPIO 10-21）
   - 编写LED指示驱动（GPIO 7）
   - **关键**：电机独立供电验证（避免电压跌落）

#### 步骤2：Mac服务端开发（5-7天）

1. **WebSocket服务端**
   - 实现FastAPI WebSocket端点
   - 接收ESP32音频流
   - 下发设备控制指令

2. **ASR集成**
   - 安装sherpa-onnx (Mac版本)
   - 下载Zipformer中文模型（25MB）
   - 编写ASR引擎封装

3. **NLU模型训练与集成**
   - 收集标注500-1000条指令数据
   - 训练CNN+LSTM模型
   - 编写NLU推理代码

4. **语音反馈**
   - 预录制20-30条常用回复
   - 编写音频播放代码（Mac端播放，或通过WebSocket发回ESP32）

#### 步骤3：端到端联调（3-5天）

1. 说指令 → ESP32采集 → Mac ASR识别 → Mac NLU理解 → 设备控制
2. 测试准确率（ASR + NLU）
3. 优化延迟（目标：<2秒）

---

### 第二阶段：最终部署（路由器独立运行）

> 目标：将AI管线移植到路由器，ESP32只负责设备控制（GPIO），或路由器直接通过USB控制设备。

#### 步骤1：路由器环境准备（1-2天）

1. **刷入OpenWRT**
   - 准备128MB MIPS路由器
   - 刷入OpenWRT固件
   - 开启SSH，配置WiFi

2. **安装Python环境**
   - 安装Python3 + pip
   - 安装必要依赖（numpy, onnxruntime等）

#### 步骤2：交叉编译（3-5天）

1. **搭建MIPS交叉编译环境**
   - 下载OpenWRT SDK
   - 编译ONNX Runtime (MIPS版本)
   - 编译sherpa-onnx (MIPS版本)

2. **模型文件传输**
   - 将ASR模型（25MB）拷贝到路由器
   - 将训练好的NLU模型（2MB）拷贝到路由器

#### 步骤3：路由器端软件开发（5-7天）

1. **音频采集**
   - 路由器通过USB-I2S音频模块连接INMP441
   - 编写音频采集代码（替代ESP32的音频采集）

2. **ASR引擎移植**
   - 在路由器上运行sherpa-onnx
   - 测试中文识别准确率

3. **NLU推理移植**
   - 在路由器上运行CNN+LSTM模型
   - 测试意图理解准确率

4. **设备控制**
   - **方案A**：路由器有GPIO → 直接控制继电器
   - **方案B**：路由器无GPIO → 通过UART/WebSocket控制ESP32（ESP32只做GPIO扩展）

5. **语音反馈**
   - 路由器通过USB-I2S DAC播放预录制音频
   - 或通过UART通知ESP32播放

#### 步骤4：联调测试（3-5天）

1. 说指令 → 路由器ASR识别 → 路由器NLU理解 → 设备控制
2. 测试内存占用（目标：<50MB）
3. 测试稳定性（72小时压力测试）

---

## 八、风险与应对

| 风险 | 概率 | 影响 | 应对方案 |
|------|------|------|---------|
| 内存不足 | 高 | 严重 | 进一步裁剪模型，或用C重写 |
| MIPS兼容性问题 | 中 | 严重 | 提前搭建交叉编译环境验证 |
| ASR准确率不高 | 中 | 中等 | 收集更多训练数据微调 |
| NLU理解错误 | 中 | 中等 | 增加训练数据，规则兜底 |
| 路由器性能差 | 高 | 中等 | 降低识别频率，增加缓存 |

---

## 九、参考项目复用清单

从 `voice-router-agent` 项目可复用：

| 文件/模块 | 复用内容 | 改动量 |
|----------|---------|--------|
| `firmware/src/main.cpp` | I2S麦克风驱动代码 | 中（改路由器的I2S接口） |
| `firmware/src/main.cpp` | 设备控制逻辑（风扇/LED） | 大（GPIO→USB-TTL） |
| `server/transport/ws_audio.py` | WebSocket协议格式 | 小（路由器直接处理） |
| `docs/开发日志.md` | 硬件接线、踩坑记录 | 无（参考） |
| `docs/hardware-shopping-list.zh-CN.md` | 硬件采购清单 | 无（参考） |

---

## 十、最终建议

### 10.1 如果50MB实在跑不下

**建议升级硬件：**
- **256MB内存路由器**（如小米AX3000T）：内存翻倍，可跑完整功能
- **树莓派Zero 2 W**（~¥200）：4核1GHz，512MB内存，轻松跑
- **RK3566开发板**（~¥150）：专用NPU，适合AI推理

### 10.2 如果坚持用128MB路由器

**必须做的妥协：**
1. ❌ 放弃实时TTS合成 → 用预录制音频
2. ❌ 放弃唤醒词 → 用按键触发
3. ⚠️ ASR用最小的模型 → 准确率可能下降
4. ✅ 优先保证指令识别准确率

---

## 附录：技术栈总结

| 层级 | 技术选型 | 大小 | 说明 |
|------|---------|------|------|
| 操作系统 | OpenWRT | - | 路由器定制Linux |
| 编程语言 | Python3 + C | - | 性能关键部分用C |
| ASR | sherpa-onnx + Zipformer | 25MB | 中文离线识别 |
| NLU | CNN+LSTM | 2MB | 意图分类+槽位提取 |
| 设备控制 | GPIO / USB-TTL | - | 取决于路由器接口 |
| 语音反馈 | 预录制音频拼接 | 0MB运行时 | Flash存储音频文件 |
| 通信协议 | WebSocket | - | 与前端/外部设备通信 |
