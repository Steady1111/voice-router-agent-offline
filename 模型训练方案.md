# 离线语音模型训练方案

> 架构师：AI架构师  
> 日期：2026-06-23  
> 目标设备：128MB MIPS架构路由器（可用内存约50MB）

---

## 一、可行性分析：实话实说

### 1.1 你要求的 vs 现实可行的

| 模型 | 你的要求 | 现实可行性 | 原因 |
|------|---------|---------|------|
| **唤醒词** | ✅ 训练自定义 | ✅ **可行** | 只需100+条正样本 |
| **ASR** | ✅ 从零训练 | ❌ **不可行** | 需要1000+小时标注数据，成本¥50万+ |
| **指令识别** | ✅ 从零训练 | ✅ **可行** | 只需500-1000条标注数据 |

### 1.2 ASR训练的残酷现实

**为什么ASR不能从零训练？**

```
训练数据需求：
├── 中文普通话：至少1000小时标注音频（约120GB）
├── 方言/口音覆盖：每种方言至少100小时
├── 标注成本：¥5-10/句，1000小时 = ¥150-300万
├── 计算资源：8xA100 GPU，训练1-2个月
└── 技术门槛：需要语音识别专家团队
```

**结论**：ASR必须使用**开源预训练模型**（如sherpa-onnx Zipformer），我们只训练**唤醒词**和**指令识别**。

---

## 二、训练方案总览

### 2.1 需要训练的模型

| 模型 | 训练方式 | 数据需求 | 训练难度 | 部署大小 |
|------|---------|---------|---------|---------|
| **唤醒词** | 基于预训练模型微调 | 100+条正样本 | 低 | 3.3MB |
| **ASR** | ❌ **不训练**，用预训练模型 | 0 | - | 25MB |
| **指令识别** | 从零训练 | 500-1000条标注 | 中等 | 2MB |

### 2.2 训练数据收集计划

```
数据收集路线图：
├── 唤醒词数据（100条，1天）
│   ├── 正样本：你说"小T小T" 50遍
│   ├── 负样本：其他话语 50条
│   └── 数据增强：音量/语速变化 → 300条
│
├── 指令识别数据（1000条，3-5天）
│   ├── 人工录制：10人 × 100条 = 1000条
│   ├── 标注：意图 + 槽位
│   └── 数据增强：背景噪音/TTS合成 → 3000条
│
└── ASR数据（不收集，用开源）
    └── 直接使用sherpa-onnx预训练模型
```

---

## 三、唤醒词训练方案

### 3.1 技术选型

| 方案 | 模型大小 | 训练数据 | 准确率 | 推荐 |
|------|---------|---------|--------|------|
| **sherpa-onnx KWS** | 3.3MB | 100+条 | ⭐⭐⭐⭐ | ✅ **推荐** |
| ESP-SR (乐鑫) | 2MB | 500+条 | ⭐⭐⭐⭐ | ESP专用 |
| 自定义CNN | 1MB | 1000+条 | ⭐⭐⭐ | 不推荐 |

**选型理由**：
- sherpa-onnx支持MIPS架构（ONNX格式）
- 基于预训练模型微调，只需100+条数据
- 中文支持好，零误触发

### 3.2 数据收集

**正样本（唤醒词）**：
```
目标：100条有效音频
├── 你本人说"小T小T"：50遍
├── 朋友说"小T小T"：5人 × 10遍 = 50遍
└── 数据增强：
    ├── 音量变化：0.5x, 1x, 1.5x, 2x
    ├── 语速变化：0.8x, 1x, 1.2x
    └── 背景噪音：添加白噪音/音乐（SNR 10-20dB）
        → 100条 × 8 = 800条训练数据
```

**负样本（非唤醒词）**：
```
目标：500条音频
├── 日常对话：200条
├── 其他唤醒词：100条（"小爱同学"、"天猫精灵"等）
├── 环境噪音：100条（电视声、键盘声、关门声）
└── 相似发音：100条（"小提小提"、"小听小听"等）
```

### 3.3 训练代码框架

```python
# train_wake_word.py
import sherpa_onnx
import torch
import torch.nn as nn
from torch.utils.data import Dataset, DataLoader

class WakeWordDataset(Dataset):
    """唤醒词数据集"""
    def __init__(self, positive_dir, negative_dir, augment=True):
        self.positive = load_audio_files(positive_dir)  # 正样本
        self.negative = load_audio_files(negative_dir)  # 负样本
        self.augment = augment
        
    def __len__(self):
        return len(self.positive) + len(self.negative)
    
    def __getitem__(self, idx):
        if idx < len(self.positive):
            audio = self.positive[idx]
            label = 1  # 正样本
        else:
            audio = self.negative[idx - len(self.positive)]
            label = 0  # 负样本
        
        if self.augment:
            audio = augment_audio(audio)  # 音量/语速/噪音
            
        return audio, label

class WakeWordModel(nn.Module):
    """基于预训练模型的唤醒词检测器"""
    def __init__(self, pretrained_model_path):
        super(WakeWordModel, self).__init__()
        # 加载预训练模型（如sherpa-onnx的Zipformer）
        self.backbone = load_pretrained_model(pretrained_model_path)
        # 微调：只训练最后几层
        self.classifier = nn.Linear(512, 2)  # 二分类：唤醒词/非唤醒词
        
    def forward(self, x):
        features = self.backbone(x)  # 提取特征
        logits = self.classifier(features)
        return logits

def train():
    # 1. 加载数据
    dataset = WakeWordDataset('data/positive', 'data/negative')
    dataloader = DataLoader(dataset, batch_size=32, shuffle=True)
    
    # 2. 加载预训练模型
    model = WakeWordModel('models/sherpa_onnx_kws_pretrained.onnx')
    
    # 3. 冻结 backbone，只训练 classifier
    for param in model.backbone.parameters():
        param.requires_grad = False
        
    # 4. 训练
    optimizer = torch.optim.Adam(model.classifier.parameters(), lr=0.001)
    criterion = nn.CrossEntropyLoss()
    
    for epoch in range(10):
        for audio, label in dataloader:
            optimizer.zero_grad()
            logits = model(audio)
            loss = criterion(logits, label)
            loss.backward()
            optimizer.step()
        
        print(f"Epoch {epoch}, Loss: {loss.item()}")
    
    # 5. 保存模型
    torch.save(model.state_dict(), 'models/wake_word_custom.pth')
    
    # 6. 转换为ONNX格式（部署到路由器）
    torch.onnx.export(model, dummy_input, 'models/wake_word_custom.onnx')

if __name__ == '__main__':
    train()
```

### 3.4 训练流程

```
Day 1：数据收集
├── 录制100条"小T小T"音频（你 + 朋友）
├── 收集500条负样本（日常对话/其他唤醒词）
└── 数据增强（音量/语速/噪音）

Day 2：训练
├── 运行 train_wake_word.py
├── 监控训练损失（目标：<0.01）
└── 保存模型（ONNX格式）

Day 3：测试
├── 实测唤醒率（目标：>95%）
├── 实测误触发率（目标：<1/24小时）
└── 微调超参数（如学习率、batch size）
```

---

## 四、指令识别训练方案

### 4.1 技术选型

| 方案 | 模型大小 | 准确率 | 训练数据 | 推荐 |
|------|---------|--------|---------|------|
| **CNN+LSTM** | 2MB | ⭐⭐⭐⭐ | 500-1000条 | ✅ **推荐** |
| **Transformer (tiny)** | 10MB | ⭐⭐⭐⭐⭐ | 2000+条 | 太大 |
| **纯规则引擎** | 0MB | ⭐⭐⭐ | 0 | 备选 |

**选型理由**：
- CNN提取音频/文本特征，LSTM理解时序指令
- 2MB模型大小，适合50MB内存预算
- 可训练，支持自定义指令集

### 4.2 意图定义（10个常用指令）

| 意图ID | 说明 | 示例指令 | 槽位 |
|--------|------|---------|------|
| set_device_state | 开关设备 | "打开风扇"、"关闭LED" | device_type, state |
| adjust_fan_speed | 调节风扇 | "调大一点"、"风速小一点" | speed_level, direction |
| set_light_brightness | 调节灯光 | "亮度调高"、"暗一点" | brightness |
| query_device_status | 查询状态 | "风扇开着吗"、"温度多少" | device_type |
| timer_setting | 设置定时 | "10分钟后关闭"、"定时1小时" | timer_duration |
| scene_mode | 场景模式 | "睡眠模式"、"工作模式" | mode_name |
| router_reboot | 重启路由器 | "重启路由器"、"网络卡了" | - |
| router_wifi_restart | 重启WiFi | "重启WiFi"、"WiFi断了" | - |
| control_ac | 控制空调 | "打开空调"、"温度调高" | state, temperature |
| help | 帮助 | "你能做什么"、"帮助" | - |

### 4.3 槽位定义

| 槽位名 | 说明 | 示例 | 数据类型 |
|--------|------|------|---------|
| device_type | 设备类型 | fan, light, LED, relay, ac | 枚举 |
| state | 状态 | on, off, toggle | 枚举 |
| speed_level | 风速档位 | 1, 2, 3, 4, 5 | 整数 |
| brightness | 亮度 | 1-100 | 整数 |
| temperature | 温度 | 16-30 | 整数 |
| timer_duration | 定时时长 | 10分钟, 1小时 | 字符串 |
| mode_name | 场景名 | 睡眠, 工作, 离开 | 枚举 |
| direction | 调节方向 | up, down, max, min | 枚举 |

### 4.4 训练数据准备

**数据格式（JSON）**：
```json
{
  "text": "打开客厅的风扇",
  "intent": "set_device_state",
  "slots": {
    "device_type": "fan",
    "location": "客厅",
    "state": "on"
  }
}
```

**数据收集方案**：

```
方案A：人工录制（推荐）
├── 10个朋友，每人说100条指令
├── 覆盖所有意图（10个意图 × 10条 = 100条/人）
├── 覆盖所有槽位组合
└── 目标：1000条标注数据

方案B：TTS合成（辅助）
├── 用Edge TTS或Piper TTS合成指令
├── 改变音量、语速、口音
└── 数据增强：添加背景噪音

方案C：数据增强（必须）
├── 同义词替换："打开" → "开启" → "启动"
├── 语序变化："打开风扇" → "风扇打开"
└── 错误注入：模拟ASR识别错误
```

### 4.5 训练代码框架

```python
# train_nlu.py
import torch
import torch.nn as nn
from torch.utils.data import Dataset, DataLoader
from transformers import BertTokenizer

class NLUDataset(Dataset):
    """指令理解数据集"""
    def __init__(self, data_file):
        self.data = load_json(data_file)
        self.tokenizer = BertTokenizer.from_pretrained('bert-base-chinese')
        
    def __len__(self):
        return len(self.data)
    
    def __getitem__(self, idx):
        item = self.data[idx]
        text = item['text']
        intent = item['intent']
        slots = item['slots']
        
        # 文本转token
        tokens = self.tokenizer(text, padding='max_length', max_length=50, return_tensors='pt')
        
        return {
            'input_ids': tokens['input_ids'],
            'attention_mask': tokens['attention_mask'],
            'intent': intent_to_id(intent),
            'slots': slots_to_ids(slots)
        }

class NLUModel(nn.Module):
    """CNN+LSTM 指令理解模型"""
    def __init__(self, vocab_size, embedding_dim, hidden_dim, num_intents, num_slots):
        super(NLUModel, self).__init__()
        self.embedding = nn.Embedding(vocab_size, embedding_dim)
        
        # CNN提取局部特征
        self.cnn1 = nn.Conv1d(embedding_dim, 128, kernel_size=3, padding=1)
        self.cnn2 = nn.Conv1d(128, 256, kernel_size=3, padding=1)
        
        # LSTM理解时序信息
        self.lstm = nn.LSTM(256, hidden_dim, batch_first=True, bidirectional=True)
        
        # 意图分类
        self.intent_classifier = nn.Linear(hidden_dim * 2, num_intents)
        
        # 槽位提取
        self.slot_classifier = nn.Linear(hidden_dim * 2, num_slots)
        
    def forward(self, input_ids, attention_mask):
        # input_ids: [batch, seq_len]
        x = self.embedding(input_ids)  # [batch, seq_len, embedding_dim]
        x = x.transpose(1, 2)  # [batch, embedding_dim, seq_len]
        
        x = self.cnn1(x)  # [batch, 128, seq_len]
        x = self.cnn2(x)  # [batch, 256, seq_len]
        
        x = x.transpose(1, 2)  # [batch, seq_len, 256]
        lstm_out, (h_n, c_n) = self.lstm(x)
        
        # 意图分类（用最后一个隐藏状态）
        intent_logits = self.intent_classifier(h_n[-2:].transpose(0, 1).contiguous().view(x.size(0), -1))
        
        # 槽位提取（每个token的预测）
        slot_logits = self.slot_classifier(lstm_out)
        
        return intent_logits, slot_logits

def train():
    # 1. 加载数据
    dataset = NLUDataset('data/nlu_train.json')
    dataloader = DataLoader(dataset, batch_size=32, shuffle=True)
    
    # 2. 初始化模型
    model = NLUModel(
        vocab_size=21128,  # BERT词表大小
        embedding_dim=768,
        hidden_dim=256,
        num_intents=10,
        num_slots=20
    )
    
    # 3. 训练配置
    optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
    criterion_intent = nn.CrossEntropyLoss()
    criterion_slot = nn.CrossEntropyLoss()
    
    # 4. 训练循环
    for epoch in range(50):
        total_loss = 0
        for batch in dataloader:
            optimizer.zero_grad()
            
            intent_logits, slot_logits = model(
                batch['input_ids'],
                batch['attention_mask']
            )
            
            loss_intent = criterion_intent(intent_logits, batch['intent'])
            loss_slot = criterion_slot(slot_logits.transpose(1, 2), batch['slots'])
            
            loss = loss_intent + loss_slot
            loss.backward()
            optimizer.step()
            
            total_loss += loss.item()
        
        print(f"Epoch {epoch}, Loss: {total_loss / len(dataloader)}")
    
    # 5. 保存模型
    torch.save(model.state_dict(), 'models/nlu_model.pth')
    
    # 6. 模型量化（减小模型大小）
    quantize_model('models/nlu_model.pth', 'models/nlu_model_quantized.pth')
    
    # 7. 转换为ONNX（部署到路由器）
    torch.onnx.export(model, dummy_input, 'models/nlu_model.onnx')

if __name__ == '__main__':
    train()
```

### 4.6 训练流程

```
Week 1：数据收集
├── Day 1-2：录制500条指令（5个朋友 × 100条）
├── Day 3-4：录制500条指令（另外5个朋友）
├── Day 5：数据标注（意图 + 槽位）
└── Day 6-7：数据增强（同义词/语序/噪音）

Week 2：模型训练
├── Day 1：运行 train_nlu.py（50个epoch）
├── Day 2：模型量化 + ONNX转换
├── Day 3-4：测试准确率（目标：>90%）
└── Day 5-7：微调超参数（如学习率、模型结构）

Week 3：部署测试
├── Day 1-2：在Mac上测试完整链路
├── Day 3-4：交叉编译到MIPS架构
└── Day 5-7：在路由器上测试
```

---

## 五、ASR方案：使用预训练模型

### 5.1 为什么不能训练ASR？

| 原因 | 说明 |
|------|------|
| **数据需求** | 1000+小时标注音频（约120GB） |
| **标注成本** | ¥5-10/句，总成本¥150-300万 |
| **计算资源** | 8xA100 GPU，训练1-2个月 |
| **技术门槛** | 需要语音识别专家团队 |

### 5.2 使用sherpa-onnx预训练模型

**模型选择**：
```
sherpa-onnx-zipformer-zh-2023-02-13 (推荐)
├── 模型大小：25MB
├── 运行时内存：~5MB
├── 中文识别准确率：~95%
└── MIPS兼容：✅ 支持ONNX格式
```

**使用方法**：
```python
# asr/sherpa_engine.py
import sherpa_onnx

class ASREngine:
    def __init__(self, model_path):
        # 加载预训练模型
        self.model = sherpa_onnx.OfflineRecognizer.from_transducer(
            encoder=model_path + '/encoder.onnx',
            decoder=model_path + '/decoder.onnx',
            joiner=model_path + '/joiner.onnx',
            tokens=model_path + '/tokens.txt',
            num_threads=4,
            sample_rate=16000
        )
        
    def transcribe(self, audio_data):
        # audio_data: 16kHz, 16bit, mono PCM
        result = self.model.decode(audio_data)
        return result.text
```

---

## 六、部署方案

### 6.1 模型文件清单

```
models/
├── wake_word.onnx           # 唤醒词模型 (3.3MB)
├── asr/
│   ├── encoder.onnx        # ASR编码器 (15MB)
│   ├── decoder.onnx       # ASR解码器 (8MB)
│   ├── joiner.onnx        # ASR连接器 (2MB)
│   └── tokens.txt         # 词表文件 (100KB)
└── nlu_model.onnx         # 指令识别模型 (2MB)
```

**总大小**：~30MB（在50MB预算内 ✅）

### 6.2 内存预算（最终部署）

| 项目 | 占用 | 备注 |
|------|------|------|
| 系统基础 | 57MB | 内核+OpenWRT+Python |
| 唤醒词 | 5MB | 3.3MB模型+2MB运行时 |
| ASR | 30MB | 25MB模型+5MB运行时 |
| NLU | 5MB | 2MB模型+3MB运行时 |
| 音频缓冲 | 5MB | 5秒@16kHz@16bit |
| **总计** | **102MB** | **略超预算** |
| 剩余余量 | 26MB | 51%余量 ✅ |

> **优化方案**：唤醒词和ASR可以分时运行（唤醒后才启动ASR），实际峰值内存~77MB。

---

## 七、实施时间表

### 7.1 总体时间表

```
Week 1-2：唤醒词训练
├── 数据收集：100条正样本 + 500条负样本
├── 模型训练：基于sherpa-onnx微调
└── 测试验证：唤醒率>95%，误触发<1/24h

Week 3-4：指令识别训练
├── 数据收集：1000条标注指令
├── 模型训练：CNN+LSTM
└── 测试验证：准确率>90%

Week 5-6：ASR集成
├── 下载sherpa-onnx预训练模型
├── 编写ASR引擎封装
└── 测试识别准确率

Week 7-8：部署到路由器
├── 交叉编译ONNX Runtime (MIPS)
├── 模型文件传输
└── 端到端测试
```

---

## 八、成本估算

| 项目 | 成本 | 说明 |
|------|------|------|
| **数据收集** | ¥0-1000 | 找朋友帮忙（免费）或众包标注 |
| **计算资源** | ¥0-500/月 | 用自己电脑训练（免费）或租云GPU |
| **模型下载** | ¥0 | sherpa-onnx模型开源免费 |
| **路由器** | ¥100-200 | 百元MIPS路由器 |
| **总计** | **¥100-2000** | 远低于ASR从零训练（¥150万+） |

---

## 九、风险提示

| 风险 | 概率 | 影响 | 应对方案 |
|------|------|------|---------|
| 唤醒词准确率不高 | 中 | 中等 | 增加训练数据，调整模型结构 |
| 指令识别准确率不高 | 中 | 中等 | 增加训练数据，规则兜底 |
| ASR识别错误 | 高 | 严重 | 收集常见错误，添加到NLU训练数据 |
| 内存超限 | 高 | 严重 | 模型量化，分时加载 |
| MIPS兼容性问题 | 中 | 严重 | 提前验证ONNX Runtime兼容性 |

---

## 十、总结

### 10.1 你能训练什么？

✅ **唤醒词**：100条数据，1-2天训练，准确率>95%  
✅ **指令识别**：1000条数据，1-2周训练，准确率>90%  
❌ **ASR**：不能训练，必须用预训练模型

### 10.2 最终方案

```
语音输入
  ↓
唤醒词检测 (3.3MB, 微调训练)
  ↓
ASR识别 (25MB, 预训练模型)
  ↓
指令理解 (2MB, 从零训练)
  ↓
设备控制 / 语音反馈
```

### 10.3 下一步行动

1. ✅ 先跑通预研模拟架构（Mac + ESP32）
2. ✅ 收集唤醒词数据（100条）
3. ✅ 训练唤醒词模型
4. ✅ 收集指令数据（1000条）
5. ✅ 训练指令识别模型
6. ✅ 集成ASR预训练模型
7. ✅ 部署到路由器

---

**最后更新：2026-06-23**
